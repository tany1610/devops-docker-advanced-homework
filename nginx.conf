server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html

    index index.php;

    server_name localhost;

    location / {
        root   /var/www/html;
        index  index.php;
    }
}
